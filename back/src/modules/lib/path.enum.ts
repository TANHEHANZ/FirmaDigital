export enum PATH_LIB {
  BASE = "/api/",
  STATUS_JACUBITUS = BASE + "status",
  LIST_TOKEN = BASE + "token/connected",
  DATA_TOKEN = BASE + "token/data",
  UPLOAD_FILE_PDF = BASE + "token/firmar_pdf",
  VALIDAR_PDF = BASE + "validar_pdf",
}
