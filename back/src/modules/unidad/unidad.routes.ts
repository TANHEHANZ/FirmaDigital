// import { Router } from "express";
// import {
//   createUnidad,
//   deletedUnidad,
//   unidadAll,
//   updateUnidad,
// } from "./unidad.controller";
// import { validate } from "../../infraestrucure/middleware/validate";
// import { schemaUnidad } from "../../infraestrucure/DTO/unidad.dto";

// const unidadRouter = Router();
// unidadRouter.get("/", unidadAll);
// unidadRouter.post("/", validate(schemaUnidad), createUnidad);
// unidadRouter.put("/:id", validate(schemaUnidad), updateUnidad);
// unidadRouter.delete("/:id", deletedUnidad);

// export default unidadRouter;
